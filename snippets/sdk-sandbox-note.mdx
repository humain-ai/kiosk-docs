<Tip>
  During development, initialise the client with an `hk_test_…` sandbox credential instead of
  `hk_live_…`. There is no separate sandbox mode or URL — sandbox behavior comes entirely from
  the credential prefix. No AI credits are consumed and no usage is billed. See
  [Sandbox mode](/concepts/sandbox-mode).
</Tip>
