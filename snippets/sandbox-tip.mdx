<Tip>
  During development, point at the **sandbox** environment so test conversations don't consume
  production LLM credits. Set `mode: "sandbox"` in `HumainKioskClient` or use the sandbox base
  URL `https://sandbox.api.humain.ai` directly.
</Tip>
