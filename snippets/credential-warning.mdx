<Warning>
  **Never expose live credentials in client-side code.** Your `hk_live_…` token is a long-lived
  secret. In browser environments, either proxy API calls through your own server or issue an
  `hk_test_…` (sandbox) credential instead for public-facing demos. Treat it like a password.
</Warning>
