<Info>
  **Store your credential as an environment variable.** Never hard-code `hk_live_…` tokens in
  source files, dialplans, or configuration files committed to version control. Use an environment
  variable (`HUMAIN_CREDENTIAL`) or a secrets manager and inject the value at runtime.
</Info>
