openapi: 3.1.0
info:
  title: Humain Kiosk — Device API
  version: "1.0"
  description: |
    The Device API is consumed by physical kiosk terminals, VoIP adapters, and website
    widgets. All requests require a device credential issued from the Humain admin panel.

    **Base URL — production:** `https://api.humain.ai`
    **Base URL — sandbox:** `https://sandbox.api.humain.ai`

    Sandbox credentials (`hk_live_…`) work against the sandbox base URL only. Responses
    are realistic but no real LLM credits are consumed and no usage is billed.

servers:
  - url: https://api.humain.ai
    description: Production
  - url: https://sandbox.api.humain.ai
    description: Sandbox — safe for testing, no billing

security:
  - DeviceToken: []

tags:
  - name: Sessions
    description: Open, interact with, and close conversation sessions.
  - name: Voice (WebRTC)
    description: Establish real-time voice sessions over WebRTC.
  - name: Webhooks
    description: |
      Manage webhook endpoints and inspect delivery history. Part of the Admin API —
      authenticated with the admin session cookie, not a device credential.
  - name: Analytics
    description: |
      Query session and usage analytics for a workspace. Part of the Admin API —
      authenticated with the admin session cookie, not a device credential.

paths:
  /v1/sessions:
    post:
      operationId: openSession
      tags: [Sessions]
      summary: Open a session
      description: |
        Creates a new conversation session for the authenticated kiosk. Returns a
        `session_id` that must be supplied to every subsequent session request.

        The `mode` field determines which interaction pattern is available:
        - `text` — REST messages and WebSocket streaming
        - `voice` — WebRTC real-time audio
        - `voip` — SIP / VoIP telephony bridge
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/OpenSessionRequest"
            examples:
              text:
                summary: Open a text session
                value:
                  mode: text
              voice:
                summary: Open a voice session
                value:
                  mode: voice
              textWithLanguage:
                summary: Open a text session with language preference
                value:
                  mode: text
                  preferred_language: "tr"
              textWithVariables:
                summary: Open a text session with dynamic variables
                value:
                  mode: text
                  conversation_initiation_data:
                    variables:
                      customer_name: "Alice"
                      booking_ref: "TK-9021"
      responses:
        "201":
          description: Session created
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/OpenSessionResponse"
              example:
                session_id: "3fa85f64-5717-4562-b3fc-2c963f66afa6"
                detected_language: "tr"
                disclosure_text: "This conversation is handled by an AI assistant."
        "401":
          $ref: "#/components/responses/Unauthenticated"
        "402":
          $ref: "#/components/responses/PlanLimitReached"
        "403":
          $ref: "#/components/responses/Forbidden"
        "429":
          $ref: "#/components/responses/RateLimited"
        "500":
          $ref: "#/components/responses/InternalError"
        "503":
          $ref: "#/components/responses/ProviderError"

  /v1/sessions/{sid}/message:
    post:
      operationId: sendMessage
      tags: [Sessions]
      summary: Send a message
      description: |
        Sends a text input to the AI and returns the complete response. The guardrail
        pipeline runs on both the input and the output before the response is returned.

        For streaming token-by-token output, use the WebSocket endpoint instead.
      parameters:
        - $ref: "#/components/parameters/SessionId"
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/MessageRequest"
            example:
              input: "What are your opening hours?"
      responses:
        "200":
          description: AI response
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/MessageResponse"
              example:
                response: "We are open Monday to Friday, 9 AM to 6 PM."
                actions_called: []
        "400":
          $ref: "#/components/responses/InputRejected"
        "401":
          $ref: "#/components/responses/Unauthenticated"
        "403":
          $ref: "#/components/responses/Forbidden"
        "429":
          $ref: "#/components/responses/RateLimited"
        "503":
          $ref: "#/components/responses/ProviderError"

  /v1/sessions/{sid}/stream:
    get:
      operationId: streamSession
      tags: [Sessions]
      summary: Stream session (WebSocket)
      description: |
        Upgrades the HTTP connection to a WebSocket for streaming text responses
        token by token. This endpoint uses the standard HTTP `101 Switching Protocols`
        response.

        **Sending a message** — send a JSON frame:
        ```json
        {"input": "What are your opening hours?"}
        ```

        **Receiving events** — the server streams typed frames:
        ```json
        {"type": "turn_start"}
        {"type": "chunk", "content": "We "}
        {"type": "chunk", "content": "are open Monday to Friday."}
        {"type": "turn_end", "message_count": 2}
        ```

        **Errors** — the server sends a close frame with a 4xxx code on fatal errors.
      parameters:
        - $ref: "#/components/parameters/SessionId"
      responses:
        "101":
          description: Switching Protocols — WebSocket connection established
        "401":
          $ref: "#/components/responses/Unauthenticated"
        "403":
          $ref: "#/components/responses/Forbidden"

  /v1/sessions/{sid}/end:
    post:
      operationId: endSession
      tags: [Sessions]
      summary: End a session
      description: |
        Closes the session, flushes usage events, and releases server resources.
        Always call this endpoint when the conversation ends — sessions left open are
        cleaned up automatically after 4 hours of inactivity, but calling end explicitly is faster
        and more accurate for billing.
      parameters:
        - $ref: "#/components/parameters/SessionId"
      responses:
        "204":
          description: Session closed — no body returned
        "400":
          $ref: "#/components/responses/SessionEnded"
        "401":
          $ref: "#/components/responses/Unauthenticated"
        "403":
          $ref: "#/components/responses/Forbidden"

  /v1/sessions/{sid}/webrtc/offer:
    post:
      operationId: webrtcOffer
      tags: [Voice (WebRTC)]
      summary: Exchange WebRTC SDP offer
      description: |
        Sends the client's SDP offer and returns the server's SDP answer. Call this after
        `RTCPeerConnection.createOffer()` and `setLocalDescription()`. Once you receive
        the answer, call `setRemoteDescription()` on the peer connection.

        ICE candidates can be submitted concurrently via `POST /v1/sessions/{sid}/webrtc/ice`.
      parameters:
        - $ref: "#/components/parameters/SessionId"
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/WebRTCOfferRequest"
      responses:
        "200":
          description: SDP answer
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/WebRTCOfferResponse"
        "401":
          $ref: "#/components/responses/Unauthenticated"
        "403":
          $ref: "#/components/responses/Forbidden"
        "503":
          $ref: "#/components/responses/ProviderError"

  /v1/sessions/{sid}/webrtc/ice:
    post:
      operationId: submitICECandidate
      tags: [Voice (WebRTC)]
      summary: Submit ICE candidate
      description: |
        Submits a WebRTC ICE candidate discovered by the client during the connection
        establishment phase. Call this for each candidate emitted by the
        `RTCPeerConnection.onicecandidate` event.
      parameters:
        - $ref: "#/components/parameters/SessionId"
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/WebRTCICERequest"
      responses:
        "204":
          description: ICE candidate accepted — no body returned
        "401":
          $ref: "#/components/responses/Unauthenticated"
        "403":
          $ref: "#/components/responses/Forbidden"

  /api/v1/webhooks:
    get:
      operationId: listWebhookEndpoints
      tags: [Webhooks]
      summary: List webhook endpoints
      security:
        - AdminSession: []
      responses:
        "200":
          description: Webhook endpoints for the workspace
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/WebhookEndpoint"
        "401":
          $ref: "#/components/responses/AdminUnauthenticated"
    post:
      operationId: createWebhookEndpoint
      tags: [Webhooks]
      summary: Create a webhook endpoint
      description: |
        Creates a new webhook endpoint. The response includes the signing secret — it is
        shown only once and cannot be retrieved again. Regenerate it from
        `POST /api/v1/webhooks/{webhook_id}/regenerate-secret` if lost.
      security:
        - AdminSession: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/CreateWebhookRequest"
            example:
              url: "https://example.com/webhooks/humain"
              event_types: [session.opened, session.ended, guardrail.fired]
              enabled: true
      responses:
        "201":
          description: Webhook endpoint created
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/WebhookEndpointWithSecret"
        "400":
          description: Invalid URL or event type
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "401":
          $ref: "#/components/responses/AdminUnauthenticated"

  /api/v1/webhooks/{webhook_id}:
    parameters:
      - $ref: "#/components/parameters/WebhookId"
    get:
      operationId: getWebhookEndpoint
      tags: [Webhooks]
      summary: Get a webhook endpoint
      security:
        - AdminSession: []
      responses:
        "200":
          description: Webhook endpoint
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/WebhookEndpoint"
        "401":
          $ref: "#/components/responses/AdminUnauthenticated"
        "404":
          $ref: "#/components/responses/NotFound"
    patch:
      operationId: updateWebhookEndpoint
      tags: [Webhooks]
      summary: Update a webhook endpoint
      security:
        - AdminSession: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/UpdateWebhookRequest"
      responses:
        "200":
          description: Webhook endpoint updated
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/WebhookEndpoint"
        "401":
          $ref: "#/components/responses/AdminUnauthenticated"
        "404":
          $ref: "#/components/responses/NotFound"
    delete:
      operationId: deleteWebhookEndpoint
      tags: [Webhooks]
      summary: Delete a webhook endpoint
      security:
        - AdminSession: []
      responses:
        "204":
          description: Webhook endpoint deleted — no body returned
        "401":
          $ref: "#/components/responses/AdminUnauthenticated"
        "404":
          $ref: "#/components/responses/NotFound"

  /api/v1/webhooks/{webhook_id}/test:
    post:
      operationId: sendTestWebhookEvent
      tags: [Webhooks]
      summary: Send a test event
      description: |
        Sends a `session.opened` payload with placeholder data to the endpoint, signed with
        its real secret. Equivalent to the **Send test event** button in the admin panel.
      parameters:
        - $ref: "#/components/parameters/WebhookId"
      security:
        - AdminSession: []
      responses:
        "202":
          description: Test event queued for delivery
        "401":
          $ref: "#/components/responses/AdminUnauthenticated"
        "404":
          $ref: "#/components/responses/NotFound"

  /api/v1/webhooks/{webhook_id}/deliveries:
    get:
      operationId: listWebhookDeliveries
      tags: [Webhooks]
      summary: List recent deliveries
      description: |
        Returns the most recent delivery attempts for the endpoint, including retries,
        for debugging failed or delayed webhooks.
      parameters:
        - $ref: "#/components/parameters/WebhookId"
      security:
        - AdminSession: []
      responses:
        "200":
          description: Recent delivery attempts, most recent first
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/WebhookDelivery"
        "401":
          $ref: "#/components/responses/AdminUnauthenticated"
        "404":
          $ref: "#/components/responses/NotFound"

  /api/v1/analytics/sessions:
    get:
      operationId: querySessionAnalytics
      tags: [Analytics]
      summary: Query session analytics
      description: |
        Returns session counts, average duration, and completion rate bucketed by day or
        hour over the requested date range.
      parameters:
        - name: from
          in: query
          required: true
          description: Start of the date range (inclusive), ISO 8601 date.
          schema:
            type: string
            format: date
        - name: to
          in: query
          required: true
          description: End of the date range (inclusive), ISO 8601 date.
          schema:
            type: string
            format: date
        - name: kiosk_id
          in: query
          required: false
          description: Filter to a single kiosk. Omit to aggregate across the workspace.
          schema:
            type: string
        - name: granularity
          in: query
          required: false
          description: Bucket size for the returned series.
          schema:
            type: string
            enum: [hour, day]
            default: day
      security:
        - AdminSession: []
      responses:
        "200":
          description: Session analytics series
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/SessionAnalyticsResponse"
        "401":
          $ref: "#/components/responses/AdminUnauthenticated"

  /api/v1/analytics/usage:
    get:
      operationId: queryUsageAnalytics
      tags: [Analytics]
      summary: Query usage analytics
      description: |
        Returns LLM token, STT minute, and TTS character consumption bucketed by day over
        the requested date range — useful for cost tracking and plan limit monitoring.
      parameters:
        - name: from
          in: query
          required: true
          schema:
            type: string
            format: date
        - name: to
          in: query
          required: true
          schema:
            type: string
            format: date
        - name: kiosk_id
          in: query
          required: false
          schema:
            type: string
      security:
        - AdminSession: []
      responses:
        "200":
          description: Usage analytics series
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/UsageAnalyticsResponse"
        "401":
          $ref: "#/components/responses/AdminUnauthenticated"

components:
  securitySchemes:
    DeviceToken:
      type: http
      scheme: bearer
      bearerFormat: hk_live_{43-char-base64url}
      description: |
        Device credential issued from the Humain admin panel under
        **Kiosk → Credentials → Issue new credential**.

        Format: `hk_live_` followed by 43 base64url characters (32 random bytes).
        The raw token is shown only once at creation time and is never stored in plain text.

    AdminSession:
      type: apiKey
      in: cookie
      name: __session
      description: |
        Admin panel session cookie (HttpOnly), issued when a workspace admin signs in.
        Webhook and analytics endpoints are part of the Admin API surface and are not
        accessible with a device credential.

  parameters:
    SessionId:
      name: sid
      in: path
      required: true
      description: Session UUID returned by `POST /v1/sessions`
      schema:
        type: string
        format: uuid
        example: "3fa85f64-5717-4562-b3fc-2c963f66afa6"

    WebhookId:
      name: webhook_id
      in: path
      required: true
      description: Webhook endpoint identifier returned by `POST /api/v1/webhooks`
      schema:
        type: string
        example: "wh_9f2a1c"

  schemas:
    OpenSessionRequest:
      type: object
      required: [mode]
      properties:
        mode:
          type: string
          enum: [text, voice, voip]
          description: |
            The session interaction mode.
            - `text` — REST + WebSocket messaging
            - `voice` — WebRTC real-time audio
            - `voip` — SIP telephony bridge
        preferred_language:
          type: string
          description: |
            BCP-47 language code for this session (e.g. `"tr"`, `"ar"`, `"de"`).
            The server uses the matching language persona if one is configured on the kiosk.
            Falls back to the kiosk's default language if no match is found.
            Omit to use the kiosk's default.
          example: "tr"
        conversation_initiation_data:
          type: object
          description: Optional structured data attached to the session at open time.
          properties:
            variables:
              type: object
              additionalProperties:
                type: string
              description: |
                Key-value pairs injected into the persona prompt as template variables.
                Keys must match a variable declared on the kiosk (**Kiosk → Persona → Variables**)
                or they are silently ignored. See [Dynamic variables](/guides/dynamic-variables).
              example:
                customer_name: "Alice"
                booking_ref: "TK-9021"

    OpenSessionResponse:
      type: object
      properties:
        session_id:
          type: string
          format: uuid
          description: Unique session identifier — pass this to all subsequent calls.
        detected_language:
          type: string
          description: |
            BCP-47 code of the language the session will use. May differ from
            `preferred_language` if the requested language is not configured on the kiosk;
            in that case the server falls back to the kiosk's default language.
          example: "tr"
        disclosure_text:
          type: string
          description: |
            EU AI Act disclosure string configured for this kiosk. Display this to the end
            user before or at the start of the conversation. Empty string when no disclosure
            is configured.
          example: "This conversation is handled by an AI assistant."

    MessageRequest:
      type: object
      required: [input]
      properties:
        input:
          type: string
          maxLength: 4096
          description: The user's text input.

    MessageResponse:
      type: object
      properties:
        response:
          type: string
          description: The AI's complete text response after guardrail processing.
        actions_called:
          type: array
          items:
            type: string
          description: |
            Names of action endpoints invoked during this turn (e.g. `fetch_availability`).
            Empty array if no tools were called.

    WebRTCOfferRequest:
      type: object
      required: [sdp]
      properties:
        sdp:
          type: string
          description: SDP offer string from `RTCPeerConnection.createOffer()`.

    WebRTCOfferResponse:
      type: object
      properties:
        sdp:
          type: string
          description: Server SDP answer — pass to `RTCPeerConnection.setRemoteDescription()`.

    WebRTCICERequest:
      type: object
      required: [candidate, sdp_mid, sdp_mline_index]
      properties:
        candidate:
          type: string
          description: ICE candidate string from `RTCPeerConnection.onicecandidate`.
        sdp_mid:
          type: string
          description: Media stream identification tag (`candidate.sdpMid`).
        sdp_mline_index:
          type: integer
          description: Index of the media description (`candidate.sdpMLineIndex`).

    WebhookEventType:
      type: string
      enum:
        - session.opened
        - session.ended
        - guardrail.fired
        - fraud.blocked
        - credential.revoked
        - kiosk.updated
        - document.processed
        - escalation.created
      description: See [Webhooks — event types](/guides/webhooks#event-types) for payload schemas.

    WebhookEndpoint:
      type: object
      properties:
        id:
          type: string
          example: "wh_9f2a1c"
        url:
          type: string
          format: uri
          example: "https://example.com/webhooks/humain"
        event_types:
          type: array
          items:
            $ref: "#/components/schemas/WebhookEventType"
        enabled:
          type: boolean
        secret_prefix:
          type: string
          description: First 8 characters of the signing secret, for identification only.
          example: "whsec_a1"
        created_at:
          type: string
          format: date-time

    WebhookEndpointWithSecret:
      allOf:
        - $ref: "#/components/schemas/WebhookEndpoint"
        - type: object
          properties:
            secret:
              type: string
              description: |
                Full HMAC signing secret. Shown only in the response to
                `POST /api/v1/webhooks` — store it immediately, it cannot be retrieved again.
              example: "whsec_a1b2c3d4e5f6..."

    CreateWebhookRequest:
      type: object
      required: [url, event_types]
      properties:
        url:
          type: string
          format: uri
          description: HTTPS URL to receive webhook deliveries.
        event_types:
          type: array
          items:
            $ref: "#/components/schemas/WebhookEventType"
          minItems: 1
        enabled:
          type: boolean
          default: true

    UpdateWebhookRequest:
      type: object
      properties:
        url:
          type: string
          format: uri
        event_types:
          type: array
          items:
            $ref: "#/components/schemas/WebhookEventType"
        enabled:
          type: boolean

    WebhookDelivery:
      type: object
      properties:
        id:
          type: string
          description: Delivery UUID — matches the `id` field in the delivered event envelope.
          example: "evt_3fa85f64571445"
        event_type:
          $ref: "#/components/schemas/WebhookEventType"
        status:
          type: string
          enum: [delivered, failed, pending]
        response_code:
          type: integer
          nullable: true
          description: HTTP status code returned by the endpoint, if a response was received.
        attempt_count:
          type: integer
        attempted_at:
          type: string
          format: date-time
        delivered_at:
          type: string
          format: date-time
          nullable: true

    SessionAnalyticsBucket:
      type: object
      properties:
        date:
          type: string
          format: date-time
        session_count:
          type: integer
        avg_duration_s:
          type: number
        completion_rate:
          type: number
          description: "Fraction of sessions that ended with `end_reason: completed`."

    SessionAnalyticsResponse:
      type: object
      properties:
        granularity:
          type: string
          enum: [hour, day]
        buckets:
          type: array
          items:
            $ref: "#/components/schemas/SessionAnalyticsBucket"

    UsageAnalyticsBucket:
      type: object
      properties:
        date:
          type: string
          format: date
        llm_tokens:
          type: integer
        stt_minutes:
          type: number
        tts_characters:
          type: integer

    UsageAnalyticsResponse:
      type: object
      properties:
        buckets:
          type: array
          items:
            $ref: "#/components/schemas/UsageAnalyticsBucket"

    Error:
      type: object
      properties:
        error:
          type: string
          description: Machine-readable error code in SCREAMING_SNAKE_CASE.
          example: UNAUTHENTICATED
        message:
          type: string
          description: Human-readable description of the error.
          example: Missing or invalid device token.
        details:
          type: object
          description: Additional error-specific fields (see individual error descriptions).

  responses:
    Unauthenticated:
      description: Missing or invalid device token.
      content:
        application/json:
          schema:
            $ref: "#/components/schemas/Error"
          example:
            error: UNAUTHENTICATED
            message: Missing or invalid device token.
            details: {}

    Forbidden:
      description: |
        Valid token but the request is not permitted. One of:
        - `KIOSK_INACTIVE` — the kiosk's status is not *active*
        - `CREDENTIAL_BLOCKED` — FraudGuard blocked this credential. When returned, the response
          includes a `Retry-After` header with the number of seconds until the block expires.
      headers:
        Retry-After:
          description: |
            Seconds until the credential block expires. Only present when `error` is
            `CREDENTIAL_BLOCKED`. Do not retry before this time has elapsed.
          schema:
            type: integer
            example: 3600
      content:
        application/json:
          schema:
            $ref: "#/components/schemas/Error"
          examples:
            kioskInactive:
              summary: Kiosk not active
              value:
                error: KIOSK_INACTIVE
                message: This kiosk is not currently active.
                details: {}
            credentialBlocked:
              summary: Credential blocked by FraudGuard
              value:
                error: CREDENTIAL_BLOCKED
                message: This credential has been blocked due to suspicious activity.
                details:
                  blocked_until: "2026-05-24T15:30:00Z"
                  reason: Excessive rate-limit violations

    PlanLimitReached:
      description: The workspace has reached its plan's session limit.
      content:
        application/json:
          schema:
            $ref: "#/components/schemas/Error"
          example:
            error: PLAN_LIMIT_REACHED
            message: Monthly session limit reached. Upgrade your plan to continue.
            details: {}

    RateLimited:
      description: |
        Too many requests. Slow down and retry after `details.retry_after_seconds`.
        A `Retry-After` header is also included with the same value for HTTP-standard clients.
      headers:
        Retry-After:
          description: Seconds to wait before retrying. Mirrors `details.retry_after_seconds`.
          schema:
            type: integer
            example: 60
      content:
        application/json:
          schema:
            $ref: "#/components/schemas/Error"
          example:
            error: RATE_LIMITED
            message: Rate limit exceeded.
            details:
              retry_after_seconds: 30

    InputRejected:
      description: |
        The input was blocked by a guardrail rule or FraudGuard pattern detection.
        `details.reason` describes why.
      content:
        application/json:
          schema:
            $ref: "#/components/schemas/Error"
          example:
            error: INPUT_REJECTED
            message: Input was rejected by a content rule.
            details:
              reason: Contains a blocked phrase.

    SessionEnded:
      description: The session has already ended or the time limit was reached.
      content:
        application/json:
          schema:
            $ref: "#/components/schemas/Error"
          example:
            error: SESSION_ENDED
            message: This session has already ended.
            details: {}

    ProviderError:
      description: |
        The LLM, STT, or TTS provider returned an error or is temporarily unavailable.
        Retry with exponential back-off.
      content:
        application/json:
          schema:
            $ref: "#/components/schemas/Error"
          example:
            error: PROVIDER_ERROR
            message: AI provider is temporarily unavailable. Please retry.
            details: {}

    InternalError:
      description: Unexpected server-side error.
      content:
        application/json:
          schema:
            $ref: "#/components/schemas/Error"
          example:
            error: INTERNAL_ERROR
            message: An unexpected error occurred.
            details: {}

    AdminUnauthenticated:
      description: Missing or invalid admin session.
      content:
        application/json:
          schema:
            $ref: "#/components/schemas/Error"
          example:
            error: UNAUTHENTICATED
            message: Missing or invalid admin session.
            details: {}

    NotFound:
      description: The requested resource does not exist in this workspace.
      content:
        application/json:
          schema:
            $ref: "#/components/schemas/Error"
          example:
            error: NOT_FOUND
            message: The requested resource was not found.
            details: {}
